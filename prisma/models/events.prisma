model Event {
  id        String   @id @default(cuid())
  sympla_event_id  String   @unique
  name      String
  active    Boolean  @default(true)
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
  last_update_date DateTime @default("1970-01-01T00:00:00.000Z")

  orders Order[]

  @@map("Events")
}
